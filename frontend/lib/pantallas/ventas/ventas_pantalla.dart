import 'package:flutter/material.dart';

class VentasPantalla extends StatelessWidget {
  const VentasPantalla({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Ventas')),
    );
  }
}